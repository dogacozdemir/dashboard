/** Persisted in strategy_logs (report_type = geo_strategy). */
export interface GeoStrategyLogContent {
  headline: string;
  summary: string;
  overallVisibilityScore: number;
  sentimentAndCitations: string;
  geoGapAnalysis: string;
  globalActionPlan: string;
  generatedAt: string;
}
